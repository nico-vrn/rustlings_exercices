// variables5.rs
//
// Execute `rustlings hint variables5` or use the `hint` watch subcommand for a
// hint.


fn main() {
    let text_number = "T-H-R-E-E"; // Déclaration de la variable textuelle initiale
    println!("Spell a Number : {}", text_number);
    
    let number = 3; // Nouvelle déclaration avec un nom différent et initialisation avec un entier
    println!("Number plus two is : {}", number + 2);
}

